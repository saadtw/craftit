// Editor type constants (converted from TypeScript)
// These are documented here for reference but not enforced at runtime.
//
// EngineConfig: { containerId, backgroundColor, autoRotate, readOnly }
// CameraState: { position: {x,y,z}, target: {x,y,z}, zoom }
// Annotation: { id, text, position: {x,y,z}, normal: {x,y,z} }
// SceneObject: { uuid, name, visible, locked }
// EditorToolState: 'move' | 'rotate' | 'scale' | 'annotate' | 'measure' | 'none'
